experiment_name: "default_r18"
seed: 42
data:
  batch_size: 32
  num_workers: 4
model:
  name: "resnet18"
  pretrained: True
  checkpoint: null
method: "default"
training:
  epochs: 50
  patience: 50
  min_delta: 0.001
  lr: 0.005
  device: "cuda"
  save_dir: "output/models"
